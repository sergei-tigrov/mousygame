# 🏆 НАСТРОЙКА РЕЙТИНГА (LEADERBOARD)

## 📋 Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      ИГРА (index.html)                      │
│                                                             │
│  - Загружает рейтинг из GitHub RAW                         │
│  - Отправляет новый скор на Vercel Function               │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ HTTP POST
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              VERCEL FUNCTION (api/save-score.js)           │
│                                                             │
│  - Валидирует данные                                       │
│  - Обновляет leaderboard.json в GitHub через API          │
│  - Токен хранится в переменных окружения (SAFE)          │
└─────────────────────────────────────────────────────────────┘
                          │
                          │ GitHub API
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              GITHUB REPOSITORY                             │
│                                                             │
│  📄 leaderboard.json - главный файл рейтинга             │
│  (содержит топ-100 скоров, автоматически сортируется)    │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 ИНСТРУКЦИЯ НАСТРОЙКИ

### Шаг 1: Создайте GitHub Personal Access Token

1. Откройте https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Даайте имя: `MouseGame Leaderboard`
4. Выберите права:
   - ✅ `public_repo` (достаточно для публичного репо)
5. Нажмите "Generate token"
6. **Скопируйте токен** (он больше не будет видимым!)

### Шаг 2: Разверните на Vercel

#### Вариант A: Через Vercel CLI (локально)

```bash
npm install -g vercel
vercel
# Следуйте инструкциям для подключения репо
```

#### Вариант B: Через Vercel Dashboard (web)

1. Откройте https://vercel.com
2. Нажмите "Add New..." → "Project"
3. Импортируйте этот репо
4. В Project Settings → Environment Variables добавьте:
   - Name: `GITHUB_TOKEN`
   - Value: `[скопированный токен из Шага 1]`
5. Нажмите "Save" → "Deploy"

### Шаг 3: Обновите URL в игре

Когда Vercel развернет функцию, вы увидите URL типа:
```
https://mousygame-name.vercel.app/api/save-score
```

В браузере консоль покажет что функция работает, или при сохранении скора вы увидите:
```
✅ Score saved: PlayerName - 1000 points
```

## ✅ ПРОВЕРКА

Откройте игру в браузере и проверьте консоль (F12):

```
✅ Рейтинг загружен (5 записей)
🏆 Отправляем скор: TestPlayer - 1000 очков (Уровень 5)
✅ Скор сохранен! Позиция: #3
📊 Рейтинг отображен (6 записей)
```

## 🛡️ БЕЗОПАСНОСТЬ

- ✅ **Токен защищен** - хранится только на Vercel сервере, никогда не видимым клиенту
- ✅ **Валидация** - Vercel функция проверяет все данные перед сохранением
- ✅ **Rate limiting** - GitHub API имеет встроенные лимиты (5000 запросов/час)
- ✅ **Git игнорирует** - файл `.gitignore` содержит `.env` и `.env.local`

## 📊 СТРУКТУРА leaderboard.json

```json
{
  "scores": [
    {
      "name": "Player Name",
      "score": 5420,
      "level": 15,
      "date": "2025-01-15"
    }
  ],
  "lastUpdated": "2025-01-15T10:00:00Z",
  "version": "1.0"
}
```

## 🔧 ЛОКАЛЬНАЯ РАЗРАБОТКА

Если хотите тестировать локально:

1. Создайте `.env.local` файл:
```
GITHUB_TOKEN=ghp_your_token_here
```

2. Установите зависимости:
```bash
npm install node-fetch@2
```

3. Запустите локально (требует Node.js):
```bash
node api/save-score.js
```

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема: "Не удалось загрузить рейтинг"
**Решение:** Проверьте интернет и консоль браузера (F12). Может быть проблема с GitHub API.

### Проблема: "403 Forbidden" при сохранении скора
**Решение:** Проверьте что GITHUB_TOKEN установлен в Vercel и имеет права `repo`.

### Проблема: "Leaderboard.json не обновляется"
**Решение:** Убедитесь что файл находится в `/leaderboard.json` в корне репо.

### Проблема: Старые скоры теряются при пуше
**Решение:** Это не произойдет! Git отслеживает изменения файла, новые скоры добавляются к существующим.

## 📞 ПОДДЕРЖКА

Если что-то не работает:

1. Проверьте консоль браузера (F12)
2. Посмотрите логи на Vercel Dashboard
3. Убедитесь что GitHub токен валидный
4. Попробуйте перезагрузить страницу

## 🎮 КАК ЭТО РАБОТАЕТ ДЛЯ ИГРОКА

1. Игрок играет
2. Игра заканчивается
3. Вводит имя и нажимает "СОХРАНИТЬ"
4. Скор отправляется на Vercel Function
5. Функция обновляет `leaderboard.json` в GitHub
6. При открытии рейтинга - загружаются актуальные данные

**Полностью прозрачно для игрока!** 🎉
